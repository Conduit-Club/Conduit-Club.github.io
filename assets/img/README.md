# 图片资源

- `brand/`：组织头像、Logo、服务器图标和 GIF。
- `screenshots/smp/`：SMP 生存服实机截图。
- `screenshots/create/`：Create 创造服实机截图。
- `screenshots/shou/`：SHOU 建筑展示服实机截图。

Docusaurus 构建后以 `/img/...` 路径引用。除本说明外，不要把图片直接放在 `assets/img/` 根目录。
